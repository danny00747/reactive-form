import { DetectErrorsPipe } from './detect-errors.pipe';

describe('DetectErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new DetectErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
