import { DisplayErrorsPipe } from './display-errors.pipe';

describe('DisplayErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
